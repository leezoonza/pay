spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

auth:
  cookie:
    secure: false

  token:
    access-token:
      secret: test-access-token-secret-must-be-at-least-32-bytes
      access-token-ttl: 15m

    refresh-token:
      refresh-token-ttl: 14d
